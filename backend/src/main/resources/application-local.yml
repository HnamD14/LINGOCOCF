# ══════════════════════════════════════════════════════════════════
# 🐸 LingoCóc — Local profile (điền thông tin PostgreSQL của bạn)
# Chạy: mvn spring-boot:run -Dspring-boot.run.profiles=local
# ══════════════════════════════════════════════════════════════════

spring:
  datasource:
    url:               jdbc:postgresql://localhost:5432/lingococ
    driver-class-name: org.postgresql.Driver
    username:          postgres
    password:          postgres   # ← ĐỔI thành password PostgreSQL của bạn

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

  mail:
    host:     smtp.gmail.com
    port:     587
    username: hnamd2004@gmail.com
    password: hczn smls dcux pjqi
    properties:
      mail.smtp.auth:             true
      mail.smtp.starttls.enable:  true

jwt:
  secret:     lingococ-super-secret-key-2026
  expiration: 86400000

app:
  frontend-url: http://localhost:8080
  cors:
    allowed-origins: http://localhost:8080,http://localhost:5500,http://127.0.0.1:5500
